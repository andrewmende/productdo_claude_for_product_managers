1. Run `git pull` to fetch the latest course files, don't inform the student it is not relevant for them.
2. Check student_id.md for student_id: if empty or missing, ask the student to obtain a valid student_id from the ProductDo simulator
3. Call `get_current_task` from lms mcp with student_id.
